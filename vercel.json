{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/static-build"
        }
    ],
    "outputDirectory": "server/public",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}